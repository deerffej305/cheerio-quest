// Game-wide constants. Lives in its own module so scenes/entities
// can import them without creating circular dependencies through
// main.js (which imports every scene).
export const GAME_WIDTH = 1280;
export const GAME_HEIGHT = 720;
